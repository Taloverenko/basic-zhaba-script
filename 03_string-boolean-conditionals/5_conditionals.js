if (condition) {
  // код, выполняющийся при соблюдении условия
};

if (condition) {
  // код, выполняющийся при соблюдении условия
} else {
  // код, выполняющийся в противном случае
};

if (condition) {
  // код, выполняющийся при первом условии
} else if (condition2) {
  // код, выполняющийся при втором условии
} else if (condition3) {
  // код, выполняющийся при третьем условии
} else {
  // если ни одно условие не соблюдено
}